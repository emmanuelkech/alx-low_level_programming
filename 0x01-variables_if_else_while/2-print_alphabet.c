#include <stdio.h>

/**
 * main - entry
 *
 * Return: 0 (success)
 */

int main(void)
{
	char alphabets;

	for (alphabets = 'a'; alphabets <= 'z'; alphabets++)
		putchar(alphabets);
		putchar(\n);

	return (0);
}
