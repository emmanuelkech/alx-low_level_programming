#include <stdio.h>

/**
 * main - entry
 *
 * Return: 0 (success)
 */

int main(void)
{
	char low_case;

	while (low_case = 'a'; low_case <= 'z'; low_case++)
		putchar(low_case);
		putchar('\n');

	return (0);
}
